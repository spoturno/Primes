#ifndef FIND_H
#define FIND_H

// counts all the prime numbers between [a, b]
// prerequisites: 0 < a < b  
int countPrimes_1(int a, int b);

//count of primes below N which can be expressed as the sum of 2 primes
int countPrimesBelowN(int n);

#endif
